# Canonical cross-GPU probe references

Saved outputs from `mosyne-bposit-probe --json` and
`mosyne-bposit-demo --strict --json` on each verified GPU. The
claim "bit-exact across NVIDIA GPUs" is verified empirically by
running the probe on a new GPU and using `--compare-with` to
diff against one of these references.

## Files

| File | GPU | Date | Notes |
|---|---|---|---|
| `probe_3090_2026-05-19.json` | RTX 3090 (Ampere, sm_86) | 2026-05-19 | Canonical bit-exactness reference; produced from `pip install -e .[torch]` with default PyTorch 2.5.1+cu124 |
| `demo_3090_2026-05-19.json` | RTX 3090 (Ampere, sm_86) | 2026-05-19 | Full demo output — all 4 claims PASS |
| `per_shape_sweep_3090_2026-05-19.json` | RTX 3090 (Ampere, sm_86) | 2026-05-19 | 11-shape latency sweep (decode_tiny → prefill_qkv_30b). Headline: bposit beats bf16 on decode shapes (decode_qkv 0.72×, decode_ffn_30b 0.67× — single-token autoregressive WIN); loses on layer-batched FFN and prefill (1.2-1.83× of bf16). Generated by `examples/per_shape_sweep.py`. |
| `probe_5090_2026-05-20.json` | RTX 5090 (Blackwell, sm_120) | 2026-05-20 | Captured with PyTorch 2.11.0+cu128 (`cu128` channel). **All 3 shapes bit-identical to the 3090 reference** — cross-architecture (Ampere ↔ Blackwell) determinism confirmed empirically. |
| `demo_5090_2026-05-20.json` | RTX 5090 (Blackwell, sm_120) | 2026-05-20 | Full demo output — all 4 claims PASS (throughput 0.67× vs fp32 / 1.17× vs bf16, 1.23% L2 rel err, 1 distinct hash over 5 runs, 0.50× weight memory). |
| `per_shape_sweep_5090_2026-05-20.json` | RTX 5090 (Blackwell, sm_120) | 2026-05-20 | 11-shape sweep. Decode wins are **stronger** than the 3090 (decode_qkv 0.479×, decode_ffn_30b 0.330×); large-MoE FFN-gate also wins (ffn_gate_30b 0.853×, ffn_gate_70b 0.871×). New Blackwell finding: the `ffn_down` (K≫N) shapes are pathological on the cuBLASLt INT8 algo-picker — ffn_down_7b 4.83×, ffn_down_30b 3.59× (vs the 3090's 1.83× worst case). decode_tiny 1.81×. |

## Verified

- **RTX 5090 (Blackwell, sm_120)** — confirmed 2026-05-20. The
  predicted bit-exactness held: the integer-only bposit-IMMA path
  plus CPU-generated fixed-seed inputs produce hashes identical to
  the 3090, across the Ampere→Blackwell architecture jump. Captured
  from an isolated `.venv-5090` (torch 2.11.0+cu128) since the
  default `cu124` PyTorch (sm_50..sm_90) cannot drive sm_120.

## Pending

- **H100 / A100 / 4090 / T4 / V100** — these are the
  community-contribution gates per `CONTRIBUTING.md`. Run the
  probe on any of these and open an issue with the JSON paste;
  we'll add it here.

## Verifying a new GPU against these references

```bash
# On the new GPU:
mosyne-bposit-probe --json --compare-with probe_3090_2026-05-19.json

# Exits 0 with "PASS: all 3 shapes bit-identical" if the cross-GPU
# claim holds for the new hardware. Exits 1 with a per-shape diff
# if any hash differs — that's a real finding, please file an issue.
```
